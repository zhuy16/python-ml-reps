# v0.6.0 - PyTorch Practice Expansion

## Added
- New bucket: PyTorch (`🔥`) with 8 hands-on practice questions covering common workflows:
  - tensor creation/ops
  - autograd
  - linear layers
  - MLP definition
  - training loop
  - Dataset/DataLoader
  - Conv2d + pooling
  - save/load state_dict
- New integrated drill in Integrated bucket:
  - Integrated PyTorch Workflow: Tensor -> DataLoader -> Train -> Evaluate
  - Includes train/val split, MLP training with BCEWithLogitsLoss + Adam, eval mode/no_grad, and save/load verification.

## Changed
- Added `torch` to `requirements.txt` so PyTorch exercises run out-of-the-box.
- Updated PyTorch save/load exercise examples to save checkpoints under `.streamlit/` instead of project root.

## Housekeeping
- Added `model.pt` to `.gitignore` to avoid accidental checkpoint commits from local practice runs.
